#!/bin/sh
# Start qemu without the qemu console, connects stdin/stdout to serial.
# Still starts the gdb server for you to attach to.
exec qemu-system-x86_64 -s -nographic -serial stdio -monitor null -cdrom cor.iso -drive file=userspace/rootfs.bin,if=virtio
