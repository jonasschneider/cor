#!/bin/sh
# Start qemu without the qemu console, connects stdin/stdout to serial.
# Still starts the gdb server for you to attach to.
exec qemu-system-x86_64 -s -nographic -serial stdio -monitor null -cdrom target/x86_64-none-elf/release/cor-x86_64-multiboot.iso -drive file=userspace/rootfs.bin,if=virtio
